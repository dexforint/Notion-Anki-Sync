Замечания:

- я утвердил тег "sport" для блока, синхронизировал данный блок, перешёл в меню к другому блоку, но данного тег "sport" я не обнаружил, хотя он явно поставлен на карточке первого блока. Перезагрузка не помогла. Хотя предыдущие теги сохранились (теги, созданные до последнего изменения кода)
- вводимый текст в поле ввода тегов не нужно сохранять при переоткрытии меню (если это не утверждённый тег). А у тебя - сохраняется.

---

Fixes:

service worker

```
const { settings } = await getState();
await saveSettings({
  ...settings,
  hiddenTags: (settings.hiddenTags || []).filter((t) => t !== "sport"),
});
```
